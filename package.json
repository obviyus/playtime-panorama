{
  "name": "playtime-panorama",
  "module": "server/index.ts",
  "scripts": {
    "dev": "bun --use-system-ca --hot server/index.ts",
    "build": "bun build --target=bun --production --outdir=dist server/index.ts",
    "start": "bun --use-system-ca dist/index.js",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "private": true,
  "type": "module",
  "dependencies": {
    "bottleneck": "^2.19.5"
  }
}
